{
  "$comment": "Contract for data/tables/family_members.csv. Checked by build/src/schema.js.",
  "title": "Family members",
  "description": "The materials a family entry stands for, in the order search lists them. A member must be an in-scope material, not another family entry (checked by the build).",
  "primaryKey": null,
  "uniqueKeys": [["FamilyMaterialID", "MemberMaterialID"]],
  "fields": [
    { "name": "FamilyMaterialID", "type": "string", "role": "editorial", "description": "The family entry.", "reference": { "table": "family_entries", "field": "MaterialID" }, "constraints": { "required": true } },
    { "name": "MemberMaterialID", "type": "string", "role": "editorial", "description": "A material it stands for.", "reference": { "table": "materials", "field": "MaterialID" }, "constraints": { "required": true } }
  ]
}
