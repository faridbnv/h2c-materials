{
  "$comment": "Contract for data/tables/reference.csv. Checked by build/src/schema.js.",
  "title": "Generic reference envelopes",
  "description": "Uncited bulk and molded envelopes from a general engineering reference, drawn on Ashby charts for scale only. Its envelopes are rows of reference_envelopes.csv, one per property.",
  "primaryKey": "Name",
  "fields": [
    {
      "name": "Category",
      "type": "string",
      "role": "raw",
      "description": "Reference category.",
      "constraints": {
        "required": true
      }
    },
    {
      "name": "Name",
      "type": "string",
      "role": "key",
      "description": "Reference material name.",
      "constraints": {
        "required": true,
        "pattern": "^.+$",
        "unique": true
      }
    }
  ]
}
